-- Runs as SYS inside FREEPDB1 during init for gvenzl images.
-- Create tables in the APP_USER schema (demo).
ALTER SESSION SET CONTAINER=FREEPDB1;

-- Switch to demo user (APP_USER gets created by env vars)
ALTER SESSION SET CURRENT_SCHEMA=DEMO;

BEGIN
  EXECUTE IMMEDIATE 'DROP TABLE DEMO_EVENTS PURGE';
EXCEPTION WHEN OTHERS THEN NULL;
END;
/

BEGIN
  EXECUTE IMMEDIATE 'DROP TABLE DEMO_PLACES PURGE';
EXCEPTION WHEN OTHERS THEN NULL;
END;
/

CREATE TABLE DEMO_PLACES (
  ID NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  NAME VARCHAR2(200) NOT NULL,
  CATEGORY VARCHAR2(50) NOT NULL,
  LAT NUMBER(10,6) NOT NULL,
  LON NUMBER(10,6) NOT NULL
);

CREATE TABLE DEMO_EVENTS (
  ID NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  DEVICE_ID VARCHAR2(64) NOT NULL,
  EVENT_TYPE VARCHAR2(50) NOT NULL,
  EVENT_TS TIMESTAMP NOT NULL,
  PAYLOAD CLOB
);

INSERT INTO DEMO_PLACES (NAME, CATEGORY, LAT, LON) VALUES
  ('Garden of the Gods', 'park', 38.878400, -104.872900);
INSERT INTO DEMO_PLACES (NAME, CATEGORY, LAT, LON) VALUES
  ('Pikes Peak', 'mountain', 38.840900, -105.042300);

INSERT INTO DEMO_EVENTS (DEVICE_ID, EVENT_TYPE, EVENT_TS, PAYLOAD) VALUES
  ('dev-001', 'BOOT', SYSTIMESTAMP, '{"msg":"device started"}');
INSERT INTO DEMO_EVENTS (DEVICE_ID, EVENT_TYPE, EVENT_TS, PAYLOAD) VALUES
  ('dev-001', 'POS',  SYSTIMESTAMP, '{"lat":38.8339,"lon":-104.8214}');

COMMIT;
